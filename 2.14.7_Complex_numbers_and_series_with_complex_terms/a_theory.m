% Complex number
% z = x + i*y

% Module
% r = |z|
% Argument
% phi = Arg(z)

% Arg(z) = arg(z) + 2*k*pi = phi + 2*k*pi

% x = r*cos(phi)
% y = r*sin(phi)

% r = |z| = sqrt(x^2+y^2)
% arg(z) = atan(y/x) + C

% Trigonometric form
% z = r*(cos(phi) + i*sin(phi))

% z1 = x1 + i*y1
% z2 = x2 + i*y2

% z1=z2, if x1=x2 and y1=y2
% z1=z2, if |z1|=|z2| and Arg(z1) = Arg(z2) + 2*k*pi

% Addition
% z1+z2 = (x1+x2)+i*(y1+y2)

% Subtraction
% z1-z2 = (x1-x2)+i*(y1-y2)

% Multiplication
% z1*z2 = (x1*x2-y2*y2) + i*(x1*y2+x2*y1)
% z1*z2=r1*r2*(cos(phi1+phi2)+i*sin(phi1+phi2))

% Division
% z1/z2=(x1*x2+y1*y2)/(x2^2+y2^2)+i*(x2*y1-x1*y2)/(x2^2+y2^2)
% z1/z2=r1/r2*(cos(phi1-phi2)+i*sin(phi1-phi2))

% Exponentiation
% z^n=r^n*(cos(n*phi)+i*sin(n*phi))

% Formula of Moivre
% (cos(phi)+i*sin(phi))^n = cos(n*phi) + i*sin(n*phi)

% Extraction of the root
% z^(1/n)=r^(1/n)*(cos((phi+2*k*pi)/n) + i*sin((phi+2*k*pi)/n))
% where k = 0,1,2,...,n-1

% exp(z*i)=cos(z)+i*sin(z)
% exp(-z*i)=cos(z)-i*sin(z)

% Euler's formulas
% cos(z)=(exp(z*i)+exp(-z*i))/2
% sin(z)=(exp(z*i)-exp(-z*i))/2i

% Exponential form
% z=r*exp(phi*i)

