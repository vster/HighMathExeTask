% Curvilinear trapezoid area
% S=int(a:b)f(x)*dx

% The area of the figure bounded by the curves
% y=f1(x) and y=f2(x), where f1(x)<=f2(x)
% S=int(a:b)(f2(x)-f1(x))*dx

% If the curve is given by parametric equations
% x=x(t) and y=y(t)
% S=int(t1:t2)y(t)*diff(x(t))*dt

% Area of the curvilinear sector in polar coordinates
% S=1/2*int(alfa:beta)ro^2*d_teta
