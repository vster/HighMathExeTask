% Body volume from known cross-sectional areas
% V=int(a:b)S(x)*dx

% Body volume of rotation
% Vx=pi*int(a:b)y^2*x

% If the figure bounded by the curves 
% y1=f1(x) and y2=f2(x) (0<=f1(x)<=f2(x))
% rotates about the axis Ox
% then body volume of rotation is
% Vx=pi*int(a:b)(y2^2-y1^2)*dx