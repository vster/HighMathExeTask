% Signs of comparison of series

% Given two series sum(n=1:inf)a(n) and sum(n=1:inf)b(n) 
% and 0<a(n)<b(n) for any n.

% If sum(n=1:inf)b(n) converges then sum(n=1:inf)a(n) converges too.
% If sum(n=1:inf)a(n) diverges then sum(n=1:inf)b(n) diverges too.

% Limit signs of comparison of series
% Given two series S1=sum(n=1:inf)a(n) and S2=sum(n=1:inf)b(n) 
% a(n) and b(n) are positive for any n.

% If 0<lim(n->inf)a(n)/n(n)<inf => both series converges or diverges
% If lim(n->inf)a(n)/b(n)=0 => S1 converges if S2 converges
% If lim(n->inf)a(n)/b(n)=inf => S1 diverges if S2 diverges

% Generalized harmonic series 
% sum(n=1:inf)1/n^p converges at p>1 and diverges at 0<p<=1


