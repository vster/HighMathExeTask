% Determine the electric field of an infinite plate with 
% a uniformly distributed charge of density sigma.

% Phi=2*epsilon0*E*S
% Q=sigma*S

% Phi = int_int (S) D*dS = sum (i) Qi
% Phi=epsilon0 int_int(S) E*dS = Q
% 2*epsilon0*E*S = sigma*S => E=sigma/(2*epsilon0)
